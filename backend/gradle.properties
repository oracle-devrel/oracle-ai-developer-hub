# Allow Gradle toolchains to auto-download a matching JDK (Java 17) for the build
org.gradle.toolchains.auto-download=true

# Optional: reduce Gradle memory pressure if needed (safe defaults)
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
